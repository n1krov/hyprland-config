# ========================================
# 🔒 HYPRLOCK CONFIG - n1krov Lock Screen
# ========================================

# ========================================
# ⚙️ GENERAL SETTINGS
# ========================================

# Aqui definimos configuraciones generales de hyprlock

general {
    disable_loading_bar = false              # Mostrar loading bar
    grace = 3                                # 3 segundos de gracia antes de pedir password
    hide_cursor = true                       # Ocultar cursor del mouse
    no_fade_in = false                       # Fade in suave
    no_fade_out = false                      # Fade out suave
    ignore_empty_input = true                # Ignorar enter sin password
}

# ========================================
# 🖼️ BACKGROUND - Fondo blur
# ========================================

background {
    monitor =                                # Para todos los monitores
    path = ~/.config/hypr/wallpapers/wallhaven-gp7eg3_1920x1080.png
    
    # 🌊 Blur effects
    blur_passes = 3                          # Intensidad del blur
    blur_size = 8                            # Tamaño del blur
    contrast = 0.8916                        # Contraste
    brightness = 0.7                         # Brillo (un poco más oscuro)
    vibrancy = 0.1696                        # Vibración de colores
    vibrancy_darkness = 0.0                  # Oscuridad de vibración
}

# ========================================
# 🕐 RELOJ PRINCIPAL
# ========================================

label {
    monitor =
    text = cmd[update:1000] echo "<big><b>$(date +'%H:%M')</b></big>"
    color = rgba(255, 255, 255, 1.0)
    font_size = 80                           # Reloj grande y visible
    font_family = Inter Bold
    position = 0, 120                        # Centrado, arriba del input
    halign = center
    valign = center
    shadow_passes = 5
    shadow_size = 10
    shadow_color = rgba(0, 0, 0, 0.5)
}

# ========================================
# 📅 FECHA
# ========================================

label {
    monitor =
    text = cmd[update:43200000] echo "<b>$(date +'%A, %d de %B')</b>"
    color = rgba(200, 200, 200, 0.9)
    font_size = 28
    font_family = Inter Medium
    position = 0, 60                         # Debajo del reloj
    halign = center
    valign = center
    shadow_passes = 3
    shadow_size = 6
    shadow_color = rgba(0, 0, 0, 0.5)
}

# ========================================
# 👤 USER GREETING
# ========================================

label {
    monitor =
    text = n1krov
    color = rgba(33, 204, 255, 1.0)         # Tu color azul característico
    font_size = 24
    font_family = Inter Medium
    position = 0, 10                        # Arriba del input
    halign = center
    valign = center
    shadow_passes = 3
    shadow_size = 6
    shadow_color = rgba(0, 0, 0, 0.6)
}

# ========================================
# 🔐 PASSWORD INPUT FIELD
# ========================================

input-field {
    monitor =
    size = 400, 60                          # Input más grande
    outline_thickness = 3
    dots_size = 0.35                        # Dots más grandes
    dots_spacing = 0.2                      # Más espaciado
    dots_center = true
    dots_rounding = -1                      # Dots redondos
    
    # 🎨 Colores (tu paleta)
    outer_color = rgba(33, 204, 255, 1.0)   # Borde azul
    inner_color = rgba(17, 17, 27, 0.95)    # Fondo oscuro
    font_color = rgba(255, 255, 255, 1.0)   # Texto blanco
    
    # 🔍 Placeholder y estados
    fade_on_empty = true
    fade_timeout = 1000
    placeholder_text = <span><i>Ingresá tu password...</i></span>
    # placeholder_text = <span color="#cdd6f4"><i>🔑 Ingresá tu password...</i></span>
    hide_input = false
    rounding = 15                           # Bordes redondeados
    
    # ✅ Estados de éxito/error
    check_color = rgba(0, 255, 153, 1.0)    # Verde para éxito
    fail_color = rgba(255, 17, 127, 1.0)    # Rosa para error
    fail_text = <span><i>$FAIL <b>($ATTEMPTS intentos)</b></i></span>

    fail_transition = 300
    
    # 🔐 Caps/Num lock
    capslock_color = rgba(255, 255, 0, 1.0)
    numlock_color = -1
    bothlock_color = -1
    invert_numlock = false
    swap_font_color = false
    
    # 📍 Posición centrada
    position = 0, -50
    halign = center
    valign = center
}

# ========================================
# 🎵 MÚSICA ACTUAL (si está reproduciéndose)
# ========================================

label {
    monitor =
    text = cmd[update:2000] echo "🎵 $(playerctl metadata --format '{{ artist }} - {{ title }}' 2>/dev/null | head -c 50 || echo 'Sin música reproduciéndose')"
    color = rgba(255, 255, 255, 0.7)
    font_size = 18
    font_family = Inter
    position = 0, -120                      # Debajo del input
    halign = center
    valign = center
    shadow_passes = 3
    shadow_size = 4
    shadow_color = rgba(0, 0, 0, 0.5)
}

# ========================================
# 🔋 BATERÍA (solo si es laptop)
# ========================================

label {
    monitor =
    text = cmd[update:10000] echo "🔋 $(cat /sys/class/power_supply/BAT*/capacity 2>/dev/null && echo '%' || echo 'Desktop Mode')"
    color = rgba(255, 255, 255, 0.6)
    font_size = 16
    font_family = Inter
    position = 0, -160                      # Esquina inferior
    halign = center
    valign = center
    shadow_passes = 2
    shadow_size = 4
    shadow_color = rgba(0, 0, 0, 0.5)
}

# ========================================
# 🌐 NETWORK STATUS
# ========================================

label {
    monitor =
    text = cmd[update:5000] echo "📶 $(nmcli -t -f active,ssid dev wifi | grep '^yes' | cut -d':' -f2 || echo 'Disconnected')"
    color = rgba(255, 255, 255, 0.5)
    font_size = 14
    font_family = Inter
    position = 0, -200
    halign = center
    valign = center
    shadow_passes = 2
    shadow_size = 4
    shadow_color = rgba(0, 0, 0, 0.5)
}
